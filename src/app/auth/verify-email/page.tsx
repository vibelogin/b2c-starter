// Generated by @vibelogin/mcp
"use client";
import { useSearchParams } from "next/navigation";
import { Suspense } from "react";
import { VerifyEmailHandler } from "@vibelogin/nextjs/components";

function VerifyHandler() {
  const params = useSearchParams();
  const token = params.get("token") || "";

  return <VerifyEmailHandler token={token} redirectAfterVerify="/" />;
}

export default function VerifyEmailPage() {
  return (
    <main style={{ minHeight: "100vh", display: "grid", placeItems: "center", backgroundColor: "#111827" }}>
      <Suspense>
        <VerifyHandler />
      </Suspense>
    </main>
  );
}
